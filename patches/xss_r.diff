diff --git a/patches/xss_r_low_original.php b/patches/xss_r_low_patched.php
index 1cbeba4..9ab5a32 100644
--- a/patches/xss_r_low_original.php
+++ b/patches/xss_r_low_patched.php
@@ -1,11 +1,16 @@
 <?php
 
-header ("X-XSS-Protection: 0");
+header("X-XSS-Protection: 1; mode=block");
 
-// Is there any input?
-if( array_key_exists( "name", $_GET ) && $_GET[ 'name' ] != NULL ) {
-	// Feedback for end user
-	$html .= '<pre>Hello ' . $_GET[ 'name' ] . '</pre>';
+if( array_key_exists("name", $_GET) && $_GET['name'] != NULL ) {
+
+    $safe_name = htmlspecialchars(
+        $_GET['name'],
+        ENT_QUOTES,
+        'UTF-8'
+    );
+
+    $html .= '<pre>Hello ' . $safe_name . '</pre>';
 }
 
-?>
+?>
\ No newline at end of file
